echo $(date +"%Y-%m-%d %H:%M:%S") $$ $PPID zsh/zsh_alias >> $HOME/.dot_log

alias deb='ssh $(debip)'
alias 14='ssh $(14ip)'
alias centos='ssh $(centosip)'

# Ignore MAILER-DAEMON message
alias from='from | tail -n +2'
